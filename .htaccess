# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# HOSTINGER CONFIGURATION
# 1. Do NOT add RewriteRule to server.js here (it causes text-display).
# 2. Set "Application Startup File" in Hostinger Panel to: backend/app.js
